spring:
    main:
        banner-mode: off
    datasource:
        url: jdbc:h2:mem:logmetrixdb
        username: sa
        password:
    h2:
        console.enabled: true
    jpa:
        defer-datasource-initialization: true
        hibernate:
            ddl-auto: create
                